# =
# RAG Backend - Configuration Minimale (Infrastructure Essentielle)
# =
# Ce fichier contient UNIQUEMENT les paramètres d'infrastructure qui ne peuvent
# pas être changés par requête API (modèles lourds, connexions, chemins)
# 
# Les paramètres de comportement (top_k, temperature, etc.) sont dans l'API
# =

# =
# SERVEUR
# =
HOST=
PORT=
RELOAD=
LOG_LEVEL=
CORS_ORIGINS=

# =
# CHEMINS (Structure du système)
# =
DATA_DIR=
CORPUS_DIR=
CACHE_DIR=

# =
# EMBEDDING MODEL (Lourd - 1.5 GB en RAM)
# =
EMBEDDING_MODEL=
EMBEDDING_DIMENSION=
EMBEDDING_DEVICE=
EMBEDDING_BATCH_SIZE=

# =
# VECTOR STORE (Base de données vectorielle)
# =
VECTOR_STORE_TYPE=
FAISS_INDEX_PATH=
LANCEDB_URI=
SQLITE_VECTOR_DB_PATH=
CHROMA_HOST=
CHROMA_PORT=

# =
# LLM (Large Language Model)
# =
LLM_BASE_URL=
LLM_MODEL=
LLM_MAX_TOKENS=
LLM_TIMEOUT=

# =
# RERANKER (Modèle de reranking - Lourd)
# =
RERANKER_MODEL=
RERANKER_DEVICE=
RERANKER_BATCH_SIZE=

# =
# CACHE (Disk-based - No Redis required)
# =
USE_CACHE=
CACHE_TTL=

# =
# VALEURS PAR DÉFAUT (Utilisées si non spécifiées dans l'API)
# =
# Ces valeurs sont des défauts. L'utilisateur peut les override dans ses requêtes API.

# Chunking par défaut (utilisé si non spécifié lors de l'ingestion)
CHUNK_SIZE=
CHUNK_OVERLAP=
CHUNKING_STRATEGY=

# Retrieval par défaut (utilisé si non spécifié dans /query)
TOP_K=
RERANK_TOP_K=
SIMILARITY_THRESHOLD=